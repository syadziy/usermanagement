# =========================
# APPLICATION
# =========================
spring:
  application:
    name: usermanagement
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  threads:
    virtual:
      enabled: true

# =========================
# DATABASE
# =========================
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/usermanagement}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin_macbook}
    hikari:
      connection-init-sql: SET TIME ZONE 'UTC'
      maximum-pool-size: ${DB_POOL_MAX_SIZE:20}
      minimum-idle: ${DB_POOL_MIN_IDLE:2}
  flyway:
    enabled: true
    locations: classpath:db/migration
    placeholders:
      defaultSuperadminPasswordHash: ${DEFAULT_SUPERADMIN_PASSWORD_HASH:}

# =========================
# KAFKA
# =========================
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 10
      properties:
        enable.idempotence: true

# =========================
# SERVER
# =========================
server:
  port: ${SERVER_PORT:9005}
  shutdown: graceful

# =========================
# USER MANAGEMENT
# =========================
usermanagement:
  jwt:
    issuer: ${JWT_ISSUER:http://localhost:9005}
    key-id: ${JWT_KEY_ID:usermanagement-local}
    private-key: ${JWT_PRIVATE_KEY:}
    public-key: ${JWT_PUBLIC_KEY:}
    generate-ephemeral-key: ${JWT_GENERATE_EPHEMERAL_KEY:false}
    audiences: ${JWT_AUDIENCES:api-gateway}
  registration:
    enabled: ${TENANT_REGISTRATION_ENABLED:true}
  audit:
    enabled: ${USERMANAGEMENT_AUDIT_ENABLED:true}
    topic: ${USERMANAGEMENT_AUDIT_TOPIC:centralized-audit.requested}
    source-system: ${USERMANAGEMENT_AUDIT_SOURCE_SYSTEM:USERMANAGEMENT-SERVICE}
    fallback-actor-id: ${USERMANAGEMENT_AUDIT_FALLBACK_ACTOR_ID:usermanagement-service}
  error-alert:
    enabled: ${USERMANAGEMENT_ERROR_ALERT_ENABLED:true}
    endpoint: ${CENTRALIZED_ALERT_URL:http://localhost:9003/api/v1/alert}
    sender-email: ${USERMANAGEMENT_ERROR_ALERT_SENDER_EMAIL:usermanagement@example.com}
    sender-name: ${USERMANAGEMENT_ERROR_ALERT_SENDER_NAME:User Management Service}
    recipients: ${USERMANAGEMENT_ERROR_ALERT_RECIPIENTS:ops@example.com}
    authorization-header: ${USERMANAGEMENT_ERROR_ALERT_AUTHORIZATION_HEADER:}
    timeout: ${USERMANAGEMENT_ERROR_ALERT_TIMEOUT:PT10S}

# =========================
# SHARED SDK
# =========================
sdk:
  timezone: ${APP_TIMEZONE:UTC}
  web:
    exception-handler:
      enabled: true
  security:
    enabled: false
    method-security-enabled: false
    cors:
      enabled: false
  logging:
    structured:
      enabled: true
      format: ecs
      auto-configure-format: true
      trace-header: X-Correlation-Id
      trace-response-header: true
    aspect:
      enabled: true
      packages: com.mac.usermanagement.service,com.mac.usermanagement.repository
  openapi:
    enabled: true
    title: User Management API
    description: Multi-tenant identity and action-level authorization service
    version: 1.0.0

# =========================
# MANAGEMENT / ACTUATOR
# =========================
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  tracing:
    sampling:
      probability: ${TRACING_SAMPLING_PROBABILITY:0.1}

# =========================
# LOGGING
# =========================
logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.mac.usermanagement: ${LOG_LEVEL_APPLICATION:INFO}
    org.springframework.security: ${LOG_LEVEL_SECURITY:INFO}
